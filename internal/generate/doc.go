// Package generate owns draft run state and provider-backed target generation.
//
// A manifest-v11 run stores configuration/style evidence, one
// reference-derived canonical subject profile, one character master, one
// semantic board per animation, complete-pose recovery evidence, a
// complete-unit review/deployment aggregate, and immutable lineage. Older
// manifests are unsupported.
package generate
