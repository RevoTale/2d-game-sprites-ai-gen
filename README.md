# 2D Game Sprites AI Generator

Schema-driven CLI for generating reusable 2D sprite assets with AI.

The generator reads `THEME.md` and `sprites.json`, expands objects into independent generation targets, and deploys only accepted target images.
